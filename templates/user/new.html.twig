{# filepath: templates/user/index.html.twig #}
{% extends 'base.html.twig' %}
{% block body %}
    <h1>Usuários</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
        {% endfor %}
    </ul>
    <a href="{{ path('user_new') }}">Novo Usuário</a>
{% endblock %}