<!DOCTYPE html>

<title>Editar tarefa</title>

{% block body %}
    <h1>Editar Tarefa</h1>

    {{ include('task/_form.html.twig', {'button_label': 'Atualizar'}) }}

    <a href="{{ path('app_task_index') }}">Voltar a lista</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
